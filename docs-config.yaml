app:
  storePath: ~/.docs-mcp-server
  telemetryEnabled: false
  embeddingModel: openai:mradermacher/llama-embed-nemotron-8b-GGUF:Q4_K_M

scraper:
  maxPages: 1000
  maxDepth: 3
  document:
    maxSize: 10485760

splitter:
  preferredChunkSize: 1500
  maxChunkSize: 8000

server:
  host: 0.0.0.0
embeddings:
  vectorDimension: 4096
